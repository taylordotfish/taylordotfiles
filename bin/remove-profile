#!/bin/sh
set -euf

if [ "$#" -ne 0 ]; then
    printf >&2 '%s\n' "usage: remove-profile"
    exit 1
fi

dispwin-quiet -c
if [ "$(xprop -root 8c '\nYES\n' _ICC_PROFILE | tail -n+2)" = YES ]; then
    dispwin-quiet -U
fi
