layout.conf is the (usually two-letter) layout code: e.g., "us" or "de".

rules/source/Makefile appends local rules to the default rules. Edit the ".add"
files and run "make".

setmap.sh runs setxkbmap with certain options and applies the generated config.
